name: QuarterFull Local Chapter Manage Diagnose

on:
  push:
    paths:
      - 'browser_publisher/quarterfull-local-chapter-manage-trigger.txt'
  workflow_dispatch:

concurrency:
  group: quarterfull-local-profile
  cancel-in-progress: false

jobs:
  diagnose:
    runs-on: [self-hosted, Windows, quarterfull]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install publisher dependencies
        working-directory: browser_publisher
        shell: cmd
        run: npm install
      - name: Diagnose QuarterFull chapter management read-only
        working-directory: browser_publisher
        shell: cmd
        run: node quarterfull_local_chapter_manage_diagnose.js
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: quarterfull-local-chapter-manage-report
          path: browser_publisher/reports/quarterfull-local-chapter-manage-diagnose.json
          retention-days: 7
